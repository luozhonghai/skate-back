<!DOCTYPE html>
<html>
<head>
  <%= render "shared/head" %>
</head>

<body class="<%= body_class(yield(:body_class)) %>">
  <\%= account_masquerade(current_account) if account_masquerade? %>
  <%= render "shared/flash" %>>
  <%= yield %>
  <%= render "shared/footer" %>>
</body>
</html>



