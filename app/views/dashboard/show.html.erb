<div class="flex flex-col sm:flex-row">
  <div class="mt-2 sm:ml-8 sm:w-1/2">
    <h1 class="mb-1 text-3xl sm:text-5xl leading-tight font-black">Hello there, <%= current_account.email %>!</h1>
    <p class="mb-8 text-xl text-gray-600">Hope you are having a great <%= DateTime.now.strftime("%A") %>.</p>

    <div>
      <span class="pill pill--tertiary"><span class="geminline-flex items-center justify-center w-4 h-4 mr-1 bg-green-500 font-black text-white rounded-full">!</span> 9 new notifications</span>
    </div>
    <%= link_to('用户列表', users_path) %>
    <%= link_to('搜索用户', dashboard_search_path) %>
  </div>
</div>